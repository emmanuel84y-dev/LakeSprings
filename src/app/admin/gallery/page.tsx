import { getAdminResource } from '@/lib/data/admin-resources'; import { ResourceForm } from '../ResourceForm'; import { ResourceDelete } from '../ResourceDelete';
export default async function Gallery(){ const rows=await getAdminResource('gallery','display_order',true); return <div><p className="eyebrow">Content</p><h1 className="mt-1 font-display text-3xl">Gallery</h1><p className="mt-2 text-sm text-ink/60">Add Supabase Storage paths here. Room images remain managed from each room.</p><div className="mt-6"><ResourceForm resource="gallery" title="Add gallery item" fields={[{name:'storage_path',label:'Storage path',required:true},{name:'category',label:'Category',type:'select',options:['hotel','rooms','restaurant','pool','facilities','events','exterior'],value:'hotel'},{name:'caption',label:'Caption'},{name:'display_order',label:'Display order',type:'number',value:0}]}/></div><div className="mt-6 overflow-x-auto rounded-xl border border-sand bg-white"><table className="w-full text-sm"><tbody className="divide-y divide-sand">{rows.map((r:any)=><tr key={r.id}><td className="px-5 py-3 font-mono text-xs">{r.storage_path}</td><td className="px-5 py-3">{r.category}</td><td className="px-5 py-3"><ResourceDelete resource="gallery" id={r.id}/></td></tr>)}</tbody></table></div></div> }
