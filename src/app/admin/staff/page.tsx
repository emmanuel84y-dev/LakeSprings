import { getAdminResource } from '@/lib/data/admin-resources'; import { ResourceForm } from '../ResourceForm';
export default async function Staff(){ const rows=await getAdminResource('profiles','created_at',false); return <div><p className="eyebrow">Access</p><h1 className="mt-1 font-display text-3xl">Staff</h1><p className="mt-2 text-sm text-ink/60">Manage roles and active status for existing Supabase Auth users. Create the Auth account in Supabase first, then add/update the profile here.</p><div className="mt-6 overflow-x-auto rounded-xl border border-sand bg-white"><table className="w-full text-sm"><thead className="border-b border-sand text-left text-xs uppercase text-ink/50"><tr><th className="p-4">Name</th><th className="p-4">Email</th><th className="p-4">Role</th><th className="p-4">Active</th><th className="p-4">Edit</th></tr></thead><tbody className="divide-y divide-sand">{rows.map((r:any)=><tr key={r.id}><td className="p-4">{r.full_name}</td><td className="p-4">{r.email}</td><td className="p-4">{r.role}</td><td className="p-4">{r.active?'Yes':'No'}</td><td className="p-4"><ResourceForm resource="profiles" id={r.id} title="Edit" fields={[{name:'full_name',label:'Name',value:r.full_name,required:true},{name:'email',label:'Email',value:r.email,required:true},{name:'role',label:'Role',type:'select',options:['staff','manager','super_admin'],value:r.role},{name:'active',label:'Active',type:'checkbox',value:r.active}]}/></td></tr>)}</tbody></table></div></div> }
