import { getAdminResource } from '@/lib/data/admin-resources'; import { ResourceForm } from '../ResourceForm';
export default async function Newsletter(){ const rows=await getAdminResource('newsletter_subscribers','subscribed_at',false); return <div><p className="eyebrow">Marketing</p><h1 className="mt-1 font-display text-3xl">Newsletter</h1><div className="mt-6 overflow-x-auto rounded-xl border border-sand bg-white"><table className="w-full text-sm"><tbody className="divide-y divide-sand">{rows.map((r:any)=><tr key={r.id}><td className="p-4">{r.name || '—'}</td><td className="p-4">{r.email}</td><td className="p-4">{r.status}</td><td className="p-4"><ResourceForm resource="newsletter_subscribers" id={r.id} title="Edit" fields={[{name:'name',label:'Name',value:r.name ?? ''},{name:'email',label:'Email',value:r.email,required:true},{name:'status',label:'Status',type:'select',options:['active','unsubscribed'],value:r.status}]}/></td></tr>)}</tbody></table></div></div> }
