import { getAdminResource } from '@/lib/data/admin-resources'; import { ResourceForm } from '../ResourceForm'; import { ResourceDelete } from '../ResourceDelete';
export default async function Testimonials(){ const rows=await getAdminResource('testimonials'); return <div><p className="eyebrow">Content</p><h1 className="mt-1 font-display text-3xl">Testimonials</h1><div className="mt-6"><ResourceForm resource="testimonials" title="Add testimonial" fields={[{name:'guest_name',label:'Guest name',required:true},{name:'location',label:'Location'},{name:'rating',label:'Rating',type:'number',value:5},{name:'review',label:'Review',type:'textarea',required:true},{name:'image_path',label:'Image path'},{name:'featured',label:'Featured',type:'checkbox'},{name:'published',label:'Published',type:'checkbox',value:true}]}/></div><div className="mt-6 space-y-3">{rows.map((r:any)=><div key={r.id} className="rounded-xl border border-sand bg-white p-5"><div className="flex justify-between"><div><b>{r.guest_name}</b><span className="ml-2 text-brass">{'★'.repeat(r.rating)}</span><p className="mt-2 text-sm text-ink/70">{r.review}</p></div><ResourceDelete resource="testimonials" id={r.id}/></div></div>)}</div></div> }
