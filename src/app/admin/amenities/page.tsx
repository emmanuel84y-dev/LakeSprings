import { getAdminResource } from '@/lib/data/admin-resources'; import { ResourceForm } from '../ResourceForm'; import { ResourceDelete } from '../ResourceDelete';
export default async function Amenities(){ const rows=await getAdminResource('amenities','name',true); return <div><p className="eyebrow">Rooms</p><h1 className="mt-1 font-display text-3xl">Amenities</h1><div className="mt-6"><ResourceForm resource="amenities" title="Add amenity" fields={[{name:'name',label:'Name',required:true},{name:'icon',label:'Icon name',value:'sparkles'},{name:'description',label:'Description',type:'textarea'},{name:'active',label:'Active',type:'checkbox',value:true}]}/></div><div className="mt-6 grid gap-3 md:grid-cols-2">{rows.map((r:any)=><div key={r.id} className="rounded-xl border border-sand bg-white p-4"><div className="flex justify-between"><div><b>{r.name}</b><p className="text-sm text-ink/60">{r.description}</p></div><ResourceDelete resource="amenities" id={r.id}/></div></div>)}</div></div> }
